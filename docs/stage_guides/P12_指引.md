# P12 阶段开发指引 - Agent Skills 能力增强

> **阶段名称**: P12 - Agent Skills 能力增强  
> **预计复杂度**: 高  
> **文档级别**: Level 2 (精简报告)  
> **前置依赖**: P11 优化完善阶段完成  
> **创建日期**: 2026-02-13

---

## 🎯 阶段目标

为游戏公司平台内部的 5 个 Agent（PM、策划、程序员、美术、测试）增加专业技能知识体系，使其在执行游戏开发任务时具备行业专家级别的专业能力。

**核心目标**：
1. **知识注入** - 将 skills.sh 生态中的专业知识提炼并注入到各 Agent
2. **按需加载** - 实现智能的知识匹配和动态加载机制
3. **质量提升** - 通过专业知识提升 Agent 的决策质量和产出水平

---

## 📊 背景说明

### 当前状态

项目中存在两个不同层面的 AI：

| 层面 | 说明 | Skills 状态 |
|------|------|-------------|
| **外层：Cursor 开发助手** | 帮助开发"游戏公司"平台的 AI（如当前的 Cursor） | ✅ 已安装 12 个 Skills |
| **内层：游戏公司 Agent** | 平台内部的 PM/策划/程序员/美术/测试 Agent | ❌ 仅有基础 Prompt，缺乏专业知识 |

**本阶段聚焦于内层 Agent 的能力增强。**

### Skills 来源

[Vercel skills.sh](https://skills.sh) 是一个开放的 Agent Skills 生态系统，包含大量专业技能包。本方案从中筛选了与游戏开发相关的专业 Skills。

---

## 🏗️ 架构方案设计

### 方案选型

经过分析，推荐采用 **方案 B（知识库 + 按需加载）为主，方案 A（System Prompt 注入）为辅** 的混合架构：

```
┌─────────────────────────────────────────────────────────────────┐
│                        Agent 运行时                              │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   System Prompt                          │   │
│  │  ┌─────────────────────────────────────────────────┐    │   │
│  │  │ L0 核心原则（约 200 字）                          │    │   │
│  │  │ - 角色定义                                       │    │   │
│  │  │ - 基本职责                                       │    │   │
│  │  │ - 核心专业原则                                   │    │   │
│  │  └─────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           +                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              动态知识加载（按任务类型）                    │   │
│  │  ┌─────────────────────────────────────────────────┐    │   │
│  │  │ 任务分析 → 匹配 Skills → 加载知识文件             │    │   │
│  │  │                                                  │    │   │
│  │  │ L1 常用知识（约 1000 字）- 默认加载              │    │   │
│  │  │ L2 深度参考（完整文档）- 按需加载                │    │   │
│  │  └─────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           ↓                                      │
│                    LLM API 调用                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 核心组件

```
backend/
├── skills/                          # 新增：Skills 知识库模块
│   ├── __init__.py
│   ├── skill_manager.py             # Skills 管理器（加载、匹配、缓存）
│   ├── skill_matcher.py             # 任务-知识匹配器
│   └── knowledge/                   # 知识文件存储
│       ├── pm/                      # PM Agent 专业知识
│       │   ├── core.md              # L0 核心原则
│       │   ├── project_management.md
│       │   ├── task_breakdown.md
│       │   └── team_coordination.md
│       ├── planner/                 # 策划 Agent 专业知识
│       │   ├── core.md
│       │   ├── game_design.md
│       │   ├── ux_principles.md
│       │   └── game_writing.md
│       ├── programmer/              # 程序员 Agent 专业知识
│       │   ├── core.md
│       │   ├── canvas_patterns.md
│       │   ├── architecture.md
│       │   ├── debugging.md
│       │   └── performance.md
│       ├── artist/                  # 美术 Agent 专业知识
│       │   ├── core.md
│       │   ├── visual_design.md
│       │   ├── pixel_art.md
│       │   └── ui_design.md
│       ├── tester/                  # 测试 Agent 专业知识
│       │   ├── core.md
│       │   ├── testing_patterns.md
│       │   ├── code_review.md
│       │   └── debugging_guide.md
│       └── shared/                  # 通用知识（所有 Agent 共享）
│           ├── agent_workflow.md
│           └── prompt_engineering.md
```

---

## 📋 各 Agent Skills 分配方案

### 1. PM Agent（项目经理）

**专业方向**：项目管理、任务分解、团队协调、进度追踪

| Skills 来源 | 知识提炼内容 | 优先级 | 安装命令 |
|-------------|-------------|--------|----------|
| `gsd` (Get Shit Done) | 结构化项目管理流程、阶段规划 | P0 | `npx skills add ctsstc/get-shit-done-skills --skill gsd` |
| `project-workflow` | 项目生命周期自动化命令 | P1 | `npx skills add jezweb/claude-skills --skill project-workflow` |
| `project-planner` | WBS 工作分解、工时估算、风险评估 | P0 | `npx skills add shubhamsaboo/awesome-llm-apps --skill project-planner` |
| `writing-plans` | 结构化执行计划撰写 | P1 | `npx skills add obra/superpowers --skill writing-plans` |
| `executing-plans` | 按计划执行追踪 | P1 | `npx skills add obra/superpowers --skill executing-plans` |
| `internal-comms` | 团队沟通模板 | P2 | `npx skills add anthropics/skills --skill internal-comms` |

**L0 核心原则（写入 System Prompt）**：
```
## PM 核心原则
1. 目标导向：始终保持项目目标清晰，不偏离用户需求
2. 任务原子化：将大任务拆解为可独立执行的小任务
3. 风险前置：识别潜在风险并在问题爆发前处理
4. 沟通透明：确保所有团队成员了解项目状态
5. 决策升级：遇到无法协调的问题，及时升级给老板（用户）
```

---

### 2. Planner Agent（游戏策划）

**专业方向**：游戏设计、玩法机制、UX 设计、数值平衡、文档撰写

| Skills 来源 | 知识提炼内容 | 优先级 | 安装命令 |
|-------------|-------------|--------|----------|
| `brainstorming` | 游戏创意头脑风暴方法 | P0 | `npx skills add obra/superpowers --skill brainstorming` |
| `doc-coauthoring` | 协作文档撰写（GDD、TDD） | P0 | `npx skills add anthropics/skills --skill doc-coauthoring` |
| `ux-researcher` | UX 研究、可用性测试、玩家体验 | P1 | `npx skills add borghei/claude-skills --skill ux-researcher` |
| `interaction-design` | 交互设计模式、用户流程设计 | P1 | `npx skills add wshobson/agents --skill interaction-design` |
| `copywriting` | 游戏文案撰写、剧情对话 | P2 | `npx skills add coreyhaines31/marketingskills --skill copywriting` |

**L0 核心原则（写入 System Prompt）**：
```
## 策划核心原则
1. 玩法优先：核心玩法要简单易懂，容易上手但有深度
2. 数值平衡：设计合理的难度曲线，避免过难或过简单
3. 文档清晰：GDD 要结构化、具体、可执行，避免模糊描述
4. 用户视角：始终从玩家角度思考体验
5. 迭代思维：接受设计需要根据测试反馈不断优化
```

---

### 3. Programmer Agent（游戏程序员）

**专业方向**：游戏编程、Canvas 渲染、架构设计、性能优化、调试

| Skills 来源 | 知识提炼内容 | 优先级 | 安装命令 |
|-------------|-------------|--------|----------|
| `canvas-design` | Canvas 设计哲学、视觉表达 | P0 | `npx skills add anthropics/skills --skill canvas-design` |
| `algorithmic-art` | 生成式图形、粒子系统、动画算法 | P1 | `npx skills add anthropics/skills --skill algorithmic-art` |
| `architecture-patterns` | 软件架构模式、模块化设计 | P0 | `npx skills add wshobson/agents --skill architecture-patterns` |
| `error-handling-patterns` | 错误处理、异常管理 | P1 | `npx skills add wshobson/agents --skill error-handling-patterns` |
| `systematic-debugging` | 系统化调试方法论 | P0 | `npx skills add obra/superpowers --skill systematic-debugging` |
| `python-performance-optimization` | Python 性能优化 | P2 | `npx skills add wshobson/agents --skill python-performance-optimization` |
| `responsive-design` | 响应式设计、屏幕适配 | P2 | `npx skills add wshobson/agents --skill responsive-design` |
| `test-driven-development` | TDD 测试驱动开发 | P1 | `npx skills add obra/superpowers --skill test-driven-development` |

**L0 核心原则（写入 System Prompt）**：
```
## 程序员核心原则
1. 代码可读：优先保证代码清晰可读，而非过度优化
2. 模块化：功能独立、接口清晰、低耦合高内聚
3. 遵守规范：读取并遵守 project_rules.yaml 和 api_registry.yaml
4. 防御性编程：处理边界情况，添加合理的错误处理
5. 复用优先：检查是否有现成代码可复用，避免重复造轮子
```

---

### 4. Artist Agent（美术设计师）

**专业方向**：像素美术、视觉设计、UI 设计、素材生成、风格一致性

| Skills 来源 | 知识提炼内容 | 优先级 | 安装命令 |
|-------------|-------------|--------|----------|
| `canvas-design` | 视觉设计哲学、美学表达 | P0 | `npx skills add anthropics/skills --skill canvas-design` |
| `algorithmic-art` | 程序化生成、动画效果 | P1 | `npx skills add anthropics/skills --skill algorithmic-art` |
| `brand-guidelines` | 品牌视觉规范、配色一致性 | P1 | `npx skills add anthropics/skills --skill brand-guidelines` |
| `visual-design-foundations` | 色彩理论、排版、构图 | P0 | `npx skills add wshobson/agents --skill visual-design-foundations` |
| `design-system-patterns` | 设计系统、UI 组件规范 | P1 | `npx skills add wshobson/agents --skill design-system-patterns` |
| `interface-design` | 界面设计、游戏 UI | P1 | `npx skills add dammyjay93/interface-design --skill interface-design` |

**L0 核心原则（写入 System Prompt）**：
```
## 美术核心原则
1. 风格一致：所有素材保持统一的像素风格和色调
2. 限制调色板：像素美术使用 16-32 色限制调色板
3. 像素精确：避免半透明抗锯齿，保持像素边缘清晰
4. 功能优先：UI 设计优先考虑可读性和易用性
5. 规范命名：素材文件遵守命名规范，便于程序调用
```

---

### 5. Tester Agent（测试工程师）

**专业方向**：功能测试、代码审查、Bug 调试、质量保障

| Skills 来源 | 知识提炼内容 | 优先级 | 安装命令 |
|-------------|-------------|--------|----------|
| `webapp-testing` | Web 应用测试框架 | P0 | `npx skills add anthropics/skills --skill webapp-testing` |
| `code-review-excellence` | 代码审查最佳实践 | P0 | `npx skills add wshobson/agents --skill code-review-excellence` |
| `systematic-debugging` | 系统化调试方法 | P0 | `npx skills add obra/superpowers --skill systematic-debugging` |
| `e2e-testing-patterns` | 端到端测试模式 | P1 | `npx skills add wshobson/agents --skill e2e-testing-patterns` |
| `verification-before-completion` | 完成前验证流程 | P1 | `npx skills add obra/superpowers --skill verification-before-completion` |
| `debugging-strategies` | 调试策略和技巧 | P1 | `npx skills add wshobson/agents --skill debugging-strategies` |

**L0 核心原则（写入 System Prompt）**：
```
## 测试核心原则
1. 实际执行：必须运行游戏代码，不能只做静态分析
2. 对照文档：根据 GDD 验证功能是否正确实现
3. 边界测试：测试极端情况和边界条件
4. Bug 可复现：Bug 报告要包含复现步骤
5. 优先级分级：Bug 按严重程度分级（阻塞/严重/一般/建议）
```

---

### 6. 全 Agent 通用 Skills

| Skills 来源 | 知识提炼内容 | 安装命令 |
|-------------|-------------|----------|
| `agentic-workflow-guide` | Agent 架构原则（SSOT、SRP）、状态机、升级框架 | `npx skills add aktsmm/agent-skills --skill agentic-workflow-guide` |
| `prompt-engineering` | Prompt 工程基础、结构化提示 | `npx skills add inference-sh/agent-skills --skill prompt-engineering` |
| `agents` (Building Agents) | Agent 架构模式（编排、路由、并行） | `npx skills add itsmostafa/llm-engineering-skills --skill agents` |

---

## 🗂️ 知识文件结构规范

### 知识分层模型

每个知识文件采用三层结构：

```markdown
# [知识点名称]

## L0 核心原则（200字以内）
> 这部分会被提炼到 System Prompt 中

- 原则 1
- 原则 2
- ...

## L1 常用知识（约 1000 字）
> 这部分默认加载到 Agent 上下文

### 子主题 1
- 要点 A
- 要点 B
...

### 子主题 2
...

## L2 深度参考
> 仅在复杂任务时按需加载，可以很详细

### 详细说明
...

### 示例
...

### 参考资料
...
```

### 知识文件示例：`pixel_art.md`

```markdown
# 像素美术设计指南

## L0 核心原则

1. **限制调色板**：使用 16-32 色的有限调色板
2. **像素精确**：避免半透明抗锯齿，保持像素边缘清晰
3. **风格统一**：所有素材保持相同的像素风格和分辨率

## L1 常用知识

### 角色尺寸规范

| 类型 | 尺寸 | 用途 |
|------|------|------|
| 最小角色 | 16x16 | NPC、小怪 |
| 标准角色 | 32x32 | 主角、重要 NPC |
| 大型角色 | 64x64 | Boss、大型敌人 |

### 动画帧数规范

| 动作类型 | 帧数 | 帧间隔(ms) |
|----------|------|------------|
| Idle（待机） | 4 | 200 |
| Walk（行走） | 6 | 100 |
| Run（跑步） | 6 | 80 |
| Attack（攻击） | 4-6 | 80 |
| Death（死亡） | 6-8 | 100 |

### 调色板设计

- **主色**：3-5 个核心色
- **强调色**：1-2 个高亮色
- **阴影色**：每个主色配 2 个阴影色阶
- **高光色**：每个主色配 1 个高光色阶

## L2 深度参考

### 像素抗锯齿技巧
（详细说明...）

### 光影渲染方法
（详细说明...）

### 动画缓动曲线
（详细说明...）
```

---

## 🔧 技术实现方案

### 1. SkillManager 类设计

```python
# backend/skills/skill_manager.py

from pathlib import Path
from typing import Dict, List, Optional
import yaml

class SkillManager:
    """Skills 知识管理器"""
    
    def __init__(self, knowledge_dir: str = "backend/skills/knowledge"):
        self.knowledge_dir = Path(knowledge_dir)
        self.cache: Dict[str, str] = {}  # 知识缓存
        
    def get_core_principles(self, agent_role: str) -> str:
        """获取 Agent 的 L0 核心原则"""
        core_file = self.knowledge_dir / agent_role / "core.md"
        if core_file.exists():
            content = self._read_file(core_file)
            # 提取 L0 部分
            return self._extract_section(content, "L0")
        return ""
    
    def get_knowledge(self, agent_role: str, skill_names: List[str], level: str = "L1") -> str:
        """
        获取指定知识内容
        
        Args:
            agent_role: Agent 角色（pm/planner/programmer/artist/tester）
            skill_names: 需要加载的知识文件名列表
            level: 知识层级（L1/L2）
        """
        knowledge = []
        for skill_name in skill_names:
            file_path = self.knowledge_dir / agent_role / f"{skill_name}.md"
            if file_path.exists():
                content = self._read_file(file_path)
                section = self._extract_section(content, level)
                if section:
                    knowledge.append(f"## {skill_name}\n\n{section}")
        return "\n\n".join(knowledge)
    
    def match_skills(self, agent_role: str, task_description: str) -> List[str]:
        """
        根据任务描述匹配合适的知识文件
        
        Args:
            agent_role: Agent 角色
            task_description: 任务描述文本
        """
        # 关键词匹配规则（可在配置文件中定义）
        skill_keywords = self._load_skill_keywords(agent_role)
        
        matched_skills = []
        task_lower = task_description.lower()
        
        for skill_name, keywords in skill_keywords.items():
            if any(kw in task_lower for kw in keywords):
                matched_skills.append(skill_name)
        
        return matched_skills
    
    def _read_file(self, file_path: Path) -> str:
        """读取文件（带缓存）"""
        cache_key = str(file_path)
        if cache_key not in self.cache:
            self.cache[cache_key] = file_path.read_text(encoding="utf-8")
        return self.cache[cache_key]
    
    def _extract_section(self, content: str, level: str) -> str:
        """提取指定层级的知识内容"""
        # 解析 Markdown，提取 ## L0/L1/L2 部分
        # ... 实现略
        pass
    
    def _load_skill_keywords(self, agent_role: str) -> Dict[str, List[str]]:
        """加载技能关键词映射"""
        keywords_file = self.knowledge_dir / agent_role / "_keywords.yaml"
        if keywords_file.exists():
            return yaml.safe_load(keywords_file.read_text())
        return {}
```

### 2. Agent 集成方案

```python
# backend/engine/agent.py 修改

from skills.skill_manager import SkillManager

class Agent:
    def __init__(self, agent_id: str, role: str):
        self.agent_id = agent_id
        self.role = role
        self.skill_manager = SkillManager()
        
        # 加载 L0 核心原则到 System Prompt
        self.core_principles = self.skill_manager.get_core_principles(role)
        self.system_prompt = self._build_system_prompt()
    
    def think_and_respond(self, message: str) -> str:
        """处理消息并生成响应"""
        
        # 1. 分析任务，匹配需要的知识
        matched_skills = self.skill_manager.match_skills(self.role, message)
        
        # 2. 加载 L1 常用知识
        knowledge_context = ""
        if matched_skills:
            knowledge_context = self.skill_manager.get_knowledge(
                self.role, matched_skills, level="L1"
            )
        
        # 3. 构建完整的消息上下文
        messages = self._build_messages(message, knowledge_context)
        
        # 4. 调用 LLM
        response = self.llm_client.chat(messages)
        
        return response
    
    def _build_messages(self, user_message: str, knowledge_context: str = "") -> list:
        """构建发送给 LLM 的消息列表"""
        messages = [
            {"role": "system", "content": self.system_prompt}
        ]
        
        # 如果有动态加载的知识，作为额外的系统消息
        if knowledge_context:
            messages.append({
                "role": "system", 
                "content": f"## 本次任务参考知识\n\n{knowledge_context}"
            })
        
        # 添加对话历史
        messages.extend(self.context.get_recent_messages())
        
        # 添加当前用户消息
        messages.append({"role": "user", "content": user_message})
        
        return messages
```

### 3. 关键词映射配置

```yaml
# backend/skills/knowledge/programmer/_keywords.yaml

canvas_patterns:
  - "canvas"
  - "渲染"
  - "绑定"
  - "动画"
  - "粒子"
  - "特效"

architecture:
  - "架构"
  - "模块"
  - "结构"
  - "重构"
  - "设计模式"

debugging:
  - "bug"
  - "错误"
  - "调试"
  - "修复"
  - "异常"
  - "报错"

performance:
  - "性能"
  - "优化"
  - "卡顿"
  - "慢"
  - "内存"
  - "fps"
```

---

## 📅 实施计划

### 阶段划分

| 子阶段 | 任务 | 预计工时 | 产出 |
|--------|------|----------|------|
| **P12.1** | Skills 知识采集与提炼 | 2-3h | 5 个角色的知识文件 |
| **P12.2** | SkillManager 模块开发 | 2-3h | skill_manager.py |
| **P12.3** | Agent 集成改造 | 2-3h | 修改后的 Agent 基类 |
| **P12.4** | 关键词匹配规则配置 | 1-2h | _keywords.yaml 文件 |
| **P12.5** | 测试与调优 | 2-3h | 测试报告 |
| **P12.6** | 文档更新 | 1h | 更新相关文档 |

**总计预计工时**：10-15 小时

### 详细任务清单

#### P12.1 Skills 知识采集与提炼

- [ ] 下载相关 Skills 的 SKILL.md 文件
- [ ] 提炼 PM Agent 知识文件（core.md, project_management.md, task_breakdown.md）
- [ ] 提炼 Planner Agent 知识文件（core.md, game_design.md, ux_principles.md）
- [ ] 提炼 Programmer Agent 知识文件（core.md, canvas_patterns.md, architecture.md, debugging.md）
- [ ] 提炼 Artist Agent 知识文件（core.md, visual_design.md, pixel_art.md, ui_design.md）
- [ ] 提炼 Tester Agent 知识文件（core.md, testing_patterns.md, code_review.md）
- [ ] 提炼通用知识文件（agent_workflow.md, prompt_engineering.md）

#### P12.2 SkillManager 模块开发

- [ ] 创建 `backend/skills/` 目录结构
- [ ] 实现 `SkillManager` 类
- [ ] 实现知识文件读取与缓存
- [ ] 实现 L0/L1/L2 分层提取
- [ ] 实现关键词匹配逻辑
- [ ] 添加单元测试

#### P12.3 Agent 集成改造

- [ ] 修改 `Agent` 基类，添加 `skill_manager` 属性
- [ ] 修改 `think_and_respond()` 方法，集成知识加载
- [ ] 更新 5 个 Agent 的 System Prompt（注入 L0 核心原则）
- [ ] 确保向后兼容（无知识文件时正常工作）

#### P12.4 关键词匹配规则配置

- [ ] 创建 PM Agent 的关键词映射
- [ ] 创建 Planner Agent 的关键词映射
- [ ] 创建 Programmer Agent 的关键词映射
- [ ] 创建 Artist Agent 的关键词映射
- [ ] 创建 Tester Agent 的关键词映射

#### P12.5 测试与调优

- [ ] 单元测试：SkillManager 各方法
- [ ] 集成测试：Agent 知识加载流程
- [ ] 端到端测试：运行完整游戏开发流程
- [ ] 性能测试：知识加载对响应时间的影响
- [ ] Token 消耗分析：对比启用前后的 Token 使用量

#### P12.6 文档更新

- [ ] 更新开发计划文档
- [ ] 更新文档索引
- [ ] 创建 P12 完成报告

---

## ⚠️ 注意事项

### Token 消耗控制

- L0 核心原则控制在 200 字以内，对 Token 影响小
- L1 常用知识控制在 1000 字以内，按需加载
- L2 深度参考仅在复杂任务时加载
- 实现知识缓存，避免重复读取

### 向后兼容

- 如果知识文件不存在，Agent 应正常工作（使用原有 Prompt）
- 新增配置项控制是否启用 Skills 增强

### 知识更新机制

- 知识文件独立于代码，便于独立更新
- 支持运行时重新加载知识（开发调试用）

---

## 📊 成功标准

1. **功能完整性**
   - [ ] 5 个 Agent 都有对应的知识文件
   - [ ] SkillManager 模块正常工作
   - [ ] Agent 能根据任务自动加载相关知识

2. **质量提升**
   - [ ] Agent 产出质量有明显提升（人工评估）
   - [ ] 减少"外行"决策（如美术 Agent 给出专业配色建议）

3. **性能可控**
   - [ ] 知识加载不影响响应速度（<100ms）
   - [ ] Token 消耗增加 < 20%

4. **可维护性**
   - [ ] 知识文件结构清晰，易于更新
   - [ ] 代码有完善的单元测试

---

## 📚 参考资料

- [skills.sh - Agent Skills 生态系统](https://skills.sh)
- [Vercel Agent Skills FAQ](https://vercel.com/blog/agent-skills-explained-an-faq)
- [Cursor Agent Skills 文档](https://cursor.com/docs/context/skills)

---

**文档版本**: v1.0  
**创建日期**: 2026-02-13  
**维护者**: AI Game Studio Team
