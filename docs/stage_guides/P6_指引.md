# P6 阶段开发指引 - 实际游戏生成

> **阶段名称**: P6 - 实际游戏生成  
> **预计复杂度**: 极高  
> **文档级别**: Level 2 (精简报告)  
> **使用说明**: AI开始P6开发时，只需阅读本文档 + platform_constitution.md
> **⚠️ 重要**: 这是核心目标阶段，必须产出可玩的游戏文件！

---

## 🎯 阶段目标

**让AI Agent真正产出可玩的游戏！**

在P4阶段，我们搭建了工作流框架，但Agent只是"说"要写代码，并没有真正调用`file_tool.write()`写文件。P6阶段的任务是：

1. **增强程序员Agent** - 让它真正调用工具写代码（.js/.html/.css文件）
2. **增强测试Agent** - 让它真正调用`code_runner`执行游戏
3. **实现Bug修复循环** - 测试失败 → 报告Bug → 程序员修复 → 重测
4. **产出可玩游戏** - `projects/{项目名}/output/` 有完整的游戏文件

**验收标准**: 用户能在浏览器中打开 `projects/snake_game/output/index.html` 玩到游戏！

---

## 📋 核心任务清单

### 必须完成的功能

#### 任务1: 增强程序员Agent的Prompt工程
- [ ] 修改 `backend/agents/programmer_agent.py`
- [ ] 在System Prompt中明确告知Agent：
  - 当前项目目录结构
  - 必须调用`self.call_tool("file", "write", file_path, code)`
  - 代码文件必须写入`projects/{项目名}/output/`
  - 提供HTML5游戏代码模板和示例
- [ ] 增强Agent的工具调用逻辑

#### 任务2: 创建代码生成提示词模板
- [ ] 创建 `backend/prompts/code_generation_template.py`
- [ ] 包含：
  - HTML5游戏标准结构
  - JavaScript游戏循环模板
  - Canvas绘制示例代码
  - 常见游戏类型的代码片段（贪吃蛇、打砖块、跑酷等）

#### 任务3: 实现实际代码写入
- [ ] 修改 `backend/workflows/game_dev_workflow.py` 的 `_phase_4_parallel_dev()`
- [ ] 程序员Agent处理任务时：
  1. 读取GDD和TDD
  2. 调用LLM生成代码
  3. **关键**: 调用`file_tool.write()`将代码写入文件
  4. 更新`api_registry.yaml`记录生成的文件
- [ ] 至少生成3个文件：
  - `index.html` - 游戏入口
  - `game.js` - 游戏主逻辑
  - `style.css` - 样式（可选）

#### 任务4: 增强测试Agent的执行能力
- [ ] 修改 `backend/agents/tester_agent.py`
- [ ] 在System Prompt中明确告知Agent：
  - 必须调用`self.call_tool("code_runner", "execute_html", ...)`
  - 检查游戏是否能成功运行
  - 如果有JavaScript错误，记录到`bug_tracker.yaml`

#### 任务5: 实现真正的游戏测试
- [ ] 修改 `backend/workflows/game_dev_workflow.py` 的 `_phase_6_testing()`
- [ ] 测试Agent处理任务时：
  1. 读取游戏文件路径
  2. **关键**: 调用`code_runner.execute_html()`执行游戏
  3. 分析执行结果（成功/失败/错误）
  4. 如果失败，写入`bug_tracker.yaml`

#### 任务6: 实现Bug修复循环
- [ ] 在 `game_dev_workflow.py` 中新增 `_phase_6_5_bug_fixing()`
- [ ] 逻辑：
  1. 读取`bug_tracker.yaml`
  2. 如果有Bug：
     - PM分配修复任务给程序员
     - 程序员读取Bug描述
     - 程序员修改代码（re-write文件）
     - 测试工程师重新测试
  3. 循环最多3次，直到无Bug或达到上限

#### 任务7: 验证游戏文件完整性
- [ ] 创建 `backend/tools/game_validator.py`
- [ ] 功能：
  - 检查`output/`目录是否有`index.html`
  - 检查JavaScript文件是否有语法错误
  - 检查游戏是否可以在浏览器中打开

---

## 🔗 前置依赖

### P3阶段已提供的工具

**FileTool** (`tools/file_tool.py`):
```python
await file_tool.write(file_path, content)  # 写入文件
await file_tool.read(file_path)            # 读取文件
await file_tool.exists(file_path)          # 检查文件是否存在
```

**CodeRunner** (`tools/code_runner.py`):
```python
result = await code_runner.execute_html(
    html_content=html_code,
    timeout=30.0
)
# 返回: {"success": bool, "output": str, "error": str}
```

**CodeSearchTool** (`tools/code_search_tool.py`):
```python
results = await code_search.search_function("movePaddle", "./output")
exists = await code_search.check_function_exists("drawGame")
```

### P4阶段的工作流框架

已有的7阶段流程：
1. 立项 - PM接收需求
2. 策划 - 策划写GDD
3. 技术设计 - 程序员写TDD
4. **并行开发** ← P6重点改造这里
5. 整合 - 简化跳过
6. **测试** ← P6重点改造这里
7. 交付 - PM汇报

**关键**: P6要把阶段4和阶段6从"假工作"变成"真工作"

---

## ✅ 验证标准

### 功能验证（必须全部通过）

- [ ] **文件产出验证**
  - `projects/{项目名}/output/index.html` 存在
  - `projects/{项目名}/output/game.js` 存在
  - 文件内容不是空的或模板
  - 文件内容是实际的游戏代码

- [ ] **代码质量验证**
  - JavaScript无语法错误
  - HTML结构完整（有`<!DOCTYPE>`、`<canvas>`等）
  - 游戏逻辑完整（有初始化、游戏循环、输入处理）

- [ ] **可玩性验证**（最重要！）
  - 在浏览器中打开`index.html`能看到游戏画面
  - 键盘/鼠标输入有响应
  - 游戏能正常运行（不崩溃）
  - 符合GDD的基本玩法

- [ ] **Bug修复验证**
  - 测试Agent能发现代码错误
  - 程序员Agent能修复Bug
  - 重测后Bug消失

### 集成验证

- [ ] 工作流7阶段仍能顺利执行
- [ ] Agent之间的消息通信正常
- [ ] WebSocket能推送进度到前端
- [ ] 项目目录结构完整

---

## 🛠️ 推荐使用的Skills

本阶段推荐激活以下Cursor Skills：
- `fastapi-templates` - 如需扩展API端点
- `async-python-patterns` - Agent异步工具调用
- `python-testing-patterns` - 工作流测试
- `logging-best-practices` - 详细日志记录
- `modern-javascript-patterns` - 生成的游戏代码质量

---

## 📐 技术要点

### 关键技术点1: 程序员Agent的Prompt设计

**错误示例（P4当前状态）**:
```python
system_prompt = "你是一名游戏程序员，负责编写游戏代码。"
# 问题：Agent只会回复"我会写代码"，不会真正调用工具
```

**正确示例（P6目标）**:
```python
system_prompt = """你是一名游戏程序员，负责编写游戏代码。

【重要】你必须调用工具来写文件，而不是仅仅回复"我会写代码"！

当前项目目录: projects/{project_name}/
输出目录: projects/{project_name}/output/

你的工作流程：
1. 阅读game_design_doc.md和tech_design_doc.md
2. 使用LLM思考游戏代码结构
3. **调用file工具写入代码**：
   await self.call_tool("file", "write", 
                        "projects/{project_name}/output/index.html",
                        html_code)
4. 至少生成3个文件：index.html、game.js、style.css
5. 回复"代码已写入output目录"

【代码模板】HTML5游戏标准结构：
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{游戏名}</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <canvas id="gameCanvas" width="800" height="600"></canvas>
    <script src="game.js"></script>
</body>
</html>

【游戏循环模板】：
const canvas = document.getElementById('gameCanvas');
const ctx = canvas.getContext('2d');

let gameState = { /* 游戏状态 */ };

function gameLoop() {
    update();  // 更新游戏逻辑
    render();  // 绘制画面
    requestAnimationFrame(gameLoop);
}

function update() { /* 更新逻辑 */ }
function render() { /* 绘制逻辑 */ }

// 启动游戏
gameLoop();

【必须使用的工具】：
- self.call_tool("file", "write", path, content)  # 写文件
- self.call_tool("code_search", "check_function_exists", name)  # 检查重复
"""
```

### 关键技术点2: Agent的工具调用增强

修改Agent基类或具体Agent，让LLM的回复能触发工具调用：

```python
# backend/agents/programmer_agent.py
class ProgrammerAgent(Agent):
    async def process_message(self, message: Dict) -> Optional[str]:
        # 1. 原有逻辑：调用LLM思考
        response = await self.think_and_respond(message['content'])
        
        # 2. 新增逻辑：解析LLM回复中的"写文件"指令
        if "写入代码" in response or "生成文件" in response:
            # 从回复中提取文件路径和内容（或重新调用LLM生成）
            files = await self._extract_files_from_response(response)
            
            for file_path, content in files:
                await self.call_tool("file", "write", file_path, content)
                self.logger.info(f"✅ 已写入文件: {file_path}")
        
        return response
    
    async def _extract_files_from_response(self, response: str) -> List[Tuple[str, str]]:
        # 方案A: 让LLM直接在回复中输出JSON格式的文件列表
        # 方案B: 再次调用LLM，明确要求生成文件内容
        # 推荐方案B：更可靠
        
        prompt = f"""根据你的计划，生成游戏代码。

你刚才说："{response}"

现在请生成实际的代码文件。输出JSON格式：
{{
    "files": [
        {{
            "path": "projects/xxx/output/index.html",
            "content": "<!DOCTYPE html>..."
        }},
        {{
            "path": "projects/xxx/output/game.js",
            "content": "const canvas = ..."
        }}
    ]
}}
"""
        llm_response = await self.llm_client.generate_response([
            {"role": "user", "content": prompt}
        ])
        
        files_data = json.loads(llm_response)
        return [(f["path"], f["content"]) for f in files_data["files"]]
```

### 关键技术点3: 测试Agent的执行增强

```python
# backend/agents/tester_agent.py
class TesterAgent(Agent):
    async def process_message(self, message: Dict) -> Optional[str]:
        if message['type'] == 'request_review' and "测试游戏" in message['content']:
            # 1. 找到游戏文件
            project_name = message.get('context', {}).get('project_name')
            html_path = f"projects/{project_name}/output/index.html"
            
            if not await self.call_tool("file", "exists", html_path):
                return "错误：未找到index.html文件"
            
            # 2. 读取游戏代码
            html_content = await self.call_tool("file", "read", html_path)
            
            # 3. **关键**：执行游戏
            result = await self.call_tool("code_runner", "execute_html", 
                                         html_content, 30.0)
            
            # 4. 分析结果
            if result['success']:
                return "✅ 游戏测试通过！能正常运行。"
            else:
                # 5. 记录Bug
                bug = {
                    "id": f"bug_{datetime.now().timestamp()}",
                    "description": result['error'],
                    "file": html_path,
                    "status": "open"
                }
                await self._write_bug_to_tracker(bug)
                return f"❌ 测试失败：{result['error']}"
```

### 关键技术点4: Bug修复循环

```python
# backend/workflows/game_dev_workflow.py
async def _phase_6_5_bug_fixing(self):
    """阶段6.5: Bug修复循环（新增）"""
    max_iterations = 3
    
    for iteration in range(max_iterations):
        self.logger.info(f"Bug修复循环 第{iteration+1}次")
        
        # 1. 读取Bug列表
        bugs = await self._read_bug_tracker()
        open_bugs = [b for b in bugs if b['status'] == 'open']
        
        if not open_bugs:
            self.logger.info("✅ 无未修复Bug，测试通过！")
            break
        
        # 2. PM分配修复任务
        for bug in open_bugs[:3]:  # 每次最多修3个Bug
            fix_message = {
                "from": "pm",
                "to": "programmer",
                "type": "request_review",
                "content": f"请修复Bug: {bug['description']}",
                "context": {"bug_id": bug['id'], "file": bug['file']}
            }
            await self.message_bus.send(fix_message)
            
            # 3. 程序员修复
            response = await self._wait_for_response("programmer", timeout=180.0)
            # （程序员Agent会重新写文件）
            
            # 4. 标记Bug为已修复
            bug['status'] = 'fixed'
        
        # 5. 更新Bug tracker
        await self._write_bug_tracker(bugs)
        
        # 6. 重新测试
        await self._phase_6_testing()
    
    if iteration == max_iterations - 1:
        self.logger.warning("⚠️ 达到最大修复次数，仍有未修复Bug")
```

---

## 🧪 测试方案

### Step 1: 快速回归测试
```bash
python tests/run_regression.py
```
验证后端功能未被破坏

### Step 2: 生成P6专项测试
AI会生成 `tests/test_p6_game_generation.py`：
- 测试程序员Agent能写文件
- 测试测试Agent能执行游戏
- 测试Bug修复循环
- 测试完整工作流产出游戏

### Step 3: 端到端验证（用户执行）
```bash
# 1. 启动后端
python backend/main.py

# 2. 创建测试项目
curl -X POST http://localhost:8000/api/project/start \
  -H "Content-Type: application/json" \
  -d '{"project_name": "test_snake", "description": "做一个简单的贪吃蛇游戏"}'

# 3. 等待工作流完成（查看WebSocket日志）

# 4. 在浏览器打开游戏
# 打开 projects/test_snake/output/index.html
```

**验收标准**:
- [ ] 浏览器能打开游戏
- [ ] 游戏能运行（有画面）
- [ ] 键盘输入有响应
- [ ] 游戏逻辑基本正确

---

## 📝 完成后的文档工作

### 1. 更新 platform_constitution.md（必做）
在P6章节记录：
- 程序员Agent的增强Prompt
- 测试Agent的执行逻辑
- Bug修复循环的实现
- 游戏文件的标准结构

### 2. 生成阶段报告（Level 2精简版）
按照 `docs/templates/report_template.md` 生成150-200行的精简报告：
- 核心改造（Agent Prompt、工具调用）
- 实际产出（生成的游戏文件示例）
- Bug修复案例
- 测试验证结果

---

## 🚀 下一阶段预告

**P7阶段将实现**: 人类介入机制（老板决策）

**本阶段需要为P7准备**:
- 决策点标记：哪些阶段需要老板审批
- 前端决策面板的后端接口
- `decision_log.yaml`的读写逻辑

---

## 💡 开发提示

### 时间分配建议
- 程序员Agent Prompt工程: 30%
- 实际文件写入逻辑: 25%
- 测试Agent执行增强: 20%
- Bug修复循环: 15%
- 测试和验证: 10%

### 开发顺序建议
1. 先改程序员Agent，让它能写一个简单的"Hello World" HTML
2. 验证文件确实被写入`output/`目录
3. 再增加游戏代码模板，生成完整游戏
4. 然后改测试Agent，让它能执行HTML
5. 实现Bug修复循环
6. 端到端测试

### 常见问题FAQ

**Q: 如果LLM生成的代码有错误怎么办？**
A: 这正是Bug修复循环的目的。测试Agent会发现错误，程序员Agent会修复。最多循环3次。

**Q: 代码生成的Prompt会不会太长？**
A: 会。但Gemini 3 Pro有100万Token上下文，完全够用。关键是Prompt要清晰。

**Q: 如果3次循环后仍有Bug？**
A: 记录到日志，继续交付。P7阶段会加入人类介入，老板可以要求重做。

**Q: 要支持多少种游戏类型？**
A: P6先支持最简单的2D游戏（贪吃蛇、打砖块、跑酷）。复杂游戏留给P10优化。

---

**指引版本**: v2.0  
**最后更新**: 2026-02-11  
**预计工作量**: 15-20轮对话（比原P6多，但是核心目标）
